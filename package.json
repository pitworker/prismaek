{
  "name": "prismaek",
  "version": "0.3.0",
  "description": "Generate colors and color schemes using maths.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "npm run lint && npm run jest",
    "jest": "jest --verbose ./tests/",
    "coverage": "jest --coverage && cat ./coverage/lcov.info | coveralls",
    "lint": "npx prettier --check .",
    "lint-fix": "echo \"Linting with Prettier...\" && npx prettier --write ."
  },
  "author": [
    "Michael Sterpka <michaelsterpka@gmail.com> (https://github.com/mster)",
    "Swan Carpenter <swanio@duck.com> (https://github.com/pitworker)"
  ],
    "license": "MIT",
  "keywords": [
    "color",
    "scheme",
    "tint",
    "tone",
    "shade",
    "harmony",
    "complementary",
    "generate"
  ],
  "jest": {
    "testRegex": "./tests/test..*.js$"
  },
  "devDependencies": {
    "coveralls": "^3.1.0",
    "jest": "^26.6.3",
    "prettier": "2.3.0"
  },
  "dependencies": {}
}
